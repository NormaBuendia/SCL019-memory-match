import shuffle from '../shuffle.js';


 describe (shuffle, () => {

    it('should be an object', () => {
         expect(typeof shuffle).toBe('object');
    });
 })

// test('cardsTotal'), () => {
//       expect('cardsTotal').toBe([]);
// };